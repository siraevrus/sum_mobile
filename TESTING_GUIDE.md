# Руководство по тестированию архивирования компании

## 🎯 Цель

Протестировать функцию архивирования/восстановления компаний в приложении Sum Warehouse Mobile.

## 📋 Предусловия

1. **Flutter SDK** установлен и настроен
2. **Эмулятор** или реальное устройство подключено
3. **Токен доступа:**
   ```
   10|JjDIZTEyAcIGRqXUbXlAook1oHMtQkPbSPxHV0dTe657fb6d
   ```
4. **API URL** настроен в приложении (обновите `lib/core/network/dio_client.dart` если нужно)

## 🚀 Запуск приложения

### Способ 1: Стандартный запуск

```bash
# Перейдите в директорию проекта
cd /Users/ruslansiraev/sum_mobile

# Получите все зависимости
flutter pub get

# Очистите кеш (опционально)
flutter clean

# Запустите приложение на эмуляторе/устройстве
flutter run

# Для отладки с дополнительной информацией
flutter run -v
```

### Способ 2: Запуск с логами

```bash
# В первом терминале запустите приложение
cd /Users/ruslansiraev/sum_mobile
flutter run

# Во втором терминале запустите логи
flutter logs
```

### Способ 3: Запуск на конкретном девайсе

```bash
# Получить список доступных устройств
flutter devices

# Запустить на конкретном устройстве
flutter run -d <device_id>
```

## 🧪 Тестовые сценарии

### Сценарий 1: Архивирование активной компании

**Шаги:**
1. Запустите приложение
2. Авторизуйтесь с предоставленным токеном
3. Перейдите в раздел "Компания" (Companies)
4. Выберите активную компанию из списка
5. На странице деталей нажмите кнопку "Архивировать" (оранжевая)
6. Подтвердите в диалоговом окне

**Ожидаемый результат:**
- ✅ Показывается сообщение "Компания '[Name]' архивирована"
- ✅ Компания помечается как архивированная на сервере
- ✅ Компания исчезает из списка активных компаний
- ✅ Логи показывают успешный запрос PUT /companies/{id}/archive

**Проверка логов:**
```bash
flutter logs | grep -i archive
# Или
flutter logs | grep "status 200"
```

### Сценарий 2: Просмотр архивированных компаний

**Шаги:**
1. После архивирования вернитесь на список компаний
2. Найдите переключатель "Показать архивированные"
3. Включите переключатель

**Ожидаемый результат:**
- ✅ Список обновится и покажет архивированные компании
- ✅ Архивированные компании помечены меткой "Архив" (оранжевая)
- ✅ API вызвал queryParam `showArchived=true`

### Сценарий 3: Восстановление архивированной компании

**Шаги:**
1. На списке архивированных компаний нажмите на архивированную компанию
2. На странице деталей нажмите "Восстановить" (зелёная кнопка)
3. Подтвердите восстановление

**Ожидаемый результат:**
- ✅ Показывается сообщение "Компания '[Name]' восстановлена"
- ✅ Компания помечается как активная на сервере
- ✅ Компания возвращается в список активных компаний
- ✅ Логи показывают успешный запрос PUT /companies/{id}/restore

### Сценарий 4: Обработка ошибок

#### 4.1 Компания не найдена (404)

**Как воспроизвести:**
1. Попробуйте архивировать несуществующую компанию (отредактируйте URL)

**Ожидаемый результат:**
- ✅ Показывается ошибка "Company not found"
- ✅ В логах: "HTTP 404"

#### 4.2 Проблема с сетью

**Как воспроизвести:**
1. Отключите интернет
2. Попробуйте архивировать компанию

**Ожидаемый результат:**
- ✅ Показывается ошибка "Проблемы с сетью. Проверьте подключение"
- ✅ Данные не изменяются на сервере

#### 4.3 Неавторизованный доступ (401)

**Как воспроизвести:**
1. Используйте неправильный/истёкший токен

**Ожидаемый результат:**
- ✅ Показывается ошибка "Требуется авторизация"
- ✅ Пользователь перенаправляется на экран логина

## 🔍 Проверка через логи

### Команды для просмотра логов:

```bash
# Все логи
flutter logs

# Только архивирование
flutter logs | grep -i archive

# Ошибки
flutter logs | grep -i error

# Сетевые запросы (Dio)
flutter logs | grep -i dio

# Комбинированный поиск
flutter logs | grep -E "(archive|error|200|PUT)"
```

### Ожидаемые логи при успехе:

```
[INFO] DioClient: PUT /companies/1/archive - Status 200
[INFO] CompanyDetailsNotifier: Loading company 1
[INFO] CompanyDetailsNotifier: Company loaded successfully
[SNACK_BAR] Компания "ООО Рога и Копыта" архивирована
```

## 🌐 Тестирование через API (curl/Postman)

### Получить список компаний:

```bash
curl -X GET "https://your-api.com/api/companies" \
  -H "Authorization: Bearer 10|JjDIZTEyAcIGRqXUbXlAook1oHMtQkPbSPxHV0dTe657fb6d" \
  -H "Accept: application/json" \
  -H "Content-Type: application/json"
```

### Архивировать компанию:

```bash
curl -X PUT "https://your-api.com/api/companies/1/archive" \
  -H "Authorization: Bearer 10|JjDIZTEyAcIGRqXUbXlAook1oHMtQkPbSPxHV0dTe657fb6d" \
  -H "Accept: application/json" \
  -H "Content-Type: application/json"
```

### Восстановить компанию:

```bash
curl -X PUT "https://your-api.com/api/companies/1/restore" \
  -H "Authorization: Bearer 10|JjDIZTEyAcIGRqXUbXlAook1oHMtQkPbSPxHV0dTe657fb6d" \
  -H "Accept: application/json" \
  -H "Content-Type: application/json"
```

### Получить детали компании:

```bash
curl -X GET "https://your-api.com/api/companies/1" \
  -H "Authorization: Bearer 10|JjDIZTEyAcIGRqXUbXlAook1oHMtQkPbSPxHV0dTe657fb6d" \
  -H "Accept: application/json"
```

## 📊 Проверка базы данных

После архивирования проверьте, что флаг `is_archived` установлен:

```sql
-- PostgreSQL/MySQL
SELECT id, name, is_archived, updated_at 
FROM companies 
WHERE id = 1;

-- Вывод должен быть:
-- id | name | is_archived | updated_at
-- 1  | ... | 1 (true) | 2024-12-XX ...
```

## 🐛 Решение типичных проблем

| Проблема | Решение |
|---------|---------|
| "No internet connection" | Проверьте интернет соединение |
| "401 Unauthorized" | Обновите токен, он мог истечь |
| "404 Not Found" | Проверьте ID компании |
| "Dart Analyzer errors" | Запустите `flutter pub get` и `flutter clean` |
| "Эмулятор не запускается" | Проверьте `flutter doctor` |
| "API возвращает 422" | Проверьте параметры запроса |

## ✅ Чек-лист успешного тестирования

- [ ] Приложение запускается без ошибок
- [ ] Успешная авторизация с предоставленным токеном
- [ ] Компании загружаются в списке
- [ ] Возможно выбрать компанию и увидеть её детали
- [ ] Нажатие "Архивировать" показывает диалог подтверждения
- [ ] После подтверждения показывается успешное сообщение
- [ ] Архивированная компания исчезает из списка активных
- [ ] Фильтр "Показать архивированные" работает
- [ ] Архивированные компании отображаются с меткой "Архив"
- [ ] Восстановление архивированной компании работает
- [ ] API запросы видны в логах с правильными статус-кодами
- [ ] Нет ошибок в консоли Flutter
- [ ] Данные синхронизированы с сервером

## 📝 Отчётность

После завершения тестирования создайте отчёт:

```markdown
# Отчёт о тестировании

## Дата: [YYYY-MM-DD]
## Тестировщик: [Ваше имя]
## Версия приложения: 1.0.0

### Результаты:
- Архивирование: ✅ / ❌
- Восстановление: ✅ / ❌
- Обработка ошибок: ✅ / ❌
- UI/UX: ✅ / ❌

### Найденные проблемы:
1. [Описание проблемы]
2. [...]

### Рекомендации:
1. [Рекомендация]
2. [...]
```

## 🎓 Дополнительные материалы

- **Реализация:** `ARCHIVE_COMPANY_IMPLEMENTATION.md`
- **Отладка:** `LOGS_DEBUGGING.md`
- **API Docs:** `API_SALES_DOCUMENTATION.md`
- **Обработка ошибок:** `ERROR_HANDLING.md`

